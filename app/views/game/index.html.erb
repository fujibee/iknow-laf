<%= render :partial => 'menu' %>

<% unless @game -%>

  <div class="panel">
    <% form_tag :action => :new do -%>
      お名前：<%= text_field_tag :name, session[:name] %>
      <%= submit_tag 'ゲームスタート！' %>
    <% end -%>
    <% javascript_tag { %> $('name').focus(); <% } %>
  </div>

<% else -%>

  <div id="inputPanel" class="panel">
    「<%= @candidate_letters %>」で始まる意味を持つ英単語は？
    <% form_tag :action => :index do -%>
      <%= text_field_tag :first_word %>
      <%= submit_tag @submit_label || 'しりとり！' %>
    <% end -%>
  </div>
  <% javascript_tag do -%>
    var inputPanel = new YAHOO.widget.Panel("inputPanel",
      { close: false, width: "300px", height: "70px", xy: [50, 60]});
    inputPanel.render();
    $('first_word').focus(); 
  <% end -%>

  <%= render :partial => "error_item" %>

  <center>
  <table>
    <tr>
      <td>
        <%= render :partial => "word_panel", :locals => {:item => @last_word_item} %>
      </td>
      <% if @first_word_item and @first_word_item.kana -%>
      <td style="background-color: transparent;">
        <%= image_tag 'arrow-right.png' %>
      </td>
      <td>
        <%= render :partial => "word_panel", :locals => {:item => @first_word_item} %>
      </td>
      <% end -%>
    </tr>
  </table>
  </center>

  <%= render :partial => 'scoreboard' %>

  <div class="menu"> 
    <%= link_to 'おしまいにする', :action => :destroy, :method => :delete %>
  </div>

<% end -%>

